#!/usr/bin/env sh
set -eu

if [ "${SKIP_AGENT_DIARY:-}" = "1" ]; then
  exit 0
fi

REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
node "$REPO_ROOT/scripts/agent-diary.js" --require-entry --source=pre-push
